<template>
    <div class="hyper-select" :style="{ maxWidth: extend ? '' : 'max-content' }">
        <select :class="{ hideText: loading }" :disabled="disabled || loading" :value="modelValue" @change="$emit('update:modelValue', $event.target.value)" ref="select">
            <slot />
        </select>

        <span class="chevron">
            <svg xmlns="http://www.w3.org/2000/svg" version="1.1" viewBox="0 0 1000 1000" width="30" height="30">
                <path d="M115.906+224.812C90.3094+225.848+65.1145+236.649+46.375+256.969C8.89599+297.609+11.4537+360.959+52.0938+398.438L431.938+748.75C470.258+784.09+529.287+784.102+567.625+748.781L947.875+398.469C988.534+361.01+991.115+297.69+953.656+257.031C916.198+216.372+852.878+213.76+812.219+251.219L499.844+539L187.812+251.25C167.492+232.51+141.503+223.777+115.906+224.812Z" />
            </svg>
        </span>

        <HyLoader v-if="loading" :width="25" :height="25" :line="3" />
    </div>
</template>

<script>
import HyLoader from "./Loader.vue";

export default {
    name: "HySelect",
    props: {
        modelValue: {
            type: String,
            required: true,
        },
        loading: {
            type: Boolean,
            default: false,
        },
        disabled: {
            type: Boolean,
            default: false,
        },
        extend: {
            type: Boolean,
            default: true,
        },
    },
    components: {
        HyLoader,
    },
};
</script>

<style lang="scss">
.hyper-select {
    margin: var(--element-margin);
    position: relative;
    min-width: max-content;

    select {
        -moz-appearance: none;
        -webkit-appearance: none;
        appearance: none;

        width: 100%;
        background-color: var(--element-secondary-bg-color);
        border-radius: var(--element-border-radius);
        padding: var(--element-padding);
        padding-right: 45px;
        font-size: var(--font-size);
        font-family: var(--font-family);
        color: var(--font-color-dark);
        cursor: pointer;
        transition: var(--element-transition);
        transition-property: background-color, color, border, filter;
        border: var(--element-border-secondary);
        box-shadow: var(--element-shadow);

        &:not([disabled]) {
            &:hover {
                background-color: var(--element-secondary-hover-bg-color);
            }
        }

        &:disabled {
            cursor: not-allowed;
            filter: opacity(50%);

            & + .chevron {
                filter: opacity(50%);
            }
        }

        &.hideText {
            color: transparent;
        }
    }

    .chevron {
        position: absolute;
        width: 20px;
        height: 20px;
        right: 15px;
        align-items: center;
        display: flex;
        margin: 0;
        top: calc(50% - 10px);
        pointer-events: none;

        svg {
            fill: var(--font-color-dark);
        }
    }

    .hyper-loader {
        position: absolute;
        top: calc(50% - 12.5px);
        left: calc(50% - 12.5px);
    }
}
</style>
