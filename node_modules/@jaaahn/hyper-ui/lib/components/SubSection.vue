<template>
    <section class="hyper-subsection">
        <h4 v-if="title">{{ title }}</h4>
        <p id="pre" v-if="pre">{{ pre }}</p>
        <slot />
        <p id="post" v-if="post">{{ post }}</p>
    </section>
</template>

<script>
export default {
    name: "HySubSection",
    props: {
        title: String,
        pre: String,
        post: String,
    },
};
</script>

<style lang="scss">
.hyper-subsection {
    background-color: var(--color-gray-3);
    border-radius: var(--section-border-radius);
    margin: var(--element-margin);
    padding: var(--element-padding);

    h4,
    #pre,
    #post {
        margin: 0 !important;
    }

    #pre,
    #post {
        font-size: 16px;
        color: gray;
    }

    & > *:first-child {
        margin-top: 0;
    }

    & > *:last-child {
        margin-bottom: 0;
    }
}
</style>
