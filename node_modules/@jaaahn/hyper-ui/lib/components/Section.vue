<template>
    <section class="hyper-section" :class="{ border: border, dividers: dividers }"><slot /></section>
</template>

<script>
export default {
    name: "HySection",
    props: {
        border: {
            type: Boolean,
            default: false,
        },
        dividers: {
            type: Boolean,
            default: false,
        },
    },
};
</script>

<style lang="scss">
.hyper-section {
    background-color: var(--section-bg-color);
    border-radius: var(--section-border-radius);
    margin: var(--element-margin);
    padding: var(--section-padding);

    &.border {
        border: var(--element-border-secondary);
    }

    & > *:first-child {
        margin-top: 0;
    }

    & > *:last-child {
        margin-bottom: 0;
    }

    &.dividers > *:not(:last-child) {
        border-bottom: var(--section-divider);
        padding-bottom: 15px; // For spacing around divider border
        margin-bottom: 15px;
    }
}
</style>
